{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "mocha test/**/*.test.js --timeout 10000 --exit",
    "test:auth": "mocha test/auth.test.js --timeout 5000 --exit",
    "test:quiz": "mocha test/quiz.test.js --timeout 10000 --exit",
    "test:modules": "mocha test/modules.test.js --timeout 5000 --exit",
    "test:dashboard": "mocha test/dashboard.test.js --timeout 5000 --exit",
    "test:integration": "mocha test/integration.test.js --timeout 15000 --exit",
    "test:coverage": "nyc mocha test/**/*.test.js --timeout 10000 --exit",
    "test:watch": "mocha test/**/*.test.js --watch --timeout 10000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-mysql-session": "^3.0.3",
    "express-session": "^1.18.2",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "chai-http": "^4.4.0",
    "mocha": "^10.2.0",
    "nodemon": "^3.1.10",
    "nyc": "^15.1.0",
    "sinon": "^17.0.1",
    "sinon-chai": "^3.7.0"
  }
}